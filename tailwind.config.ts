/** @type {import('tailwindcss').Config} */
module.exports = {
  darkMode: 'class',
  theme: {
    extend: {
      colors: {
        customColor1: '#1d4ed8', // replace with your desired color
        customColor2: '#f97316', // replace with your desired color
        // Add more custom colors as needed
      },
    },
  },
  variants: {},
  plugins: [],
};